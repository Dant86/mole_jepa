[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "imghdr"
version = "1.0.0"
description = "imghdr stdlib compatibility shim for Python >= 3.13"

[tool.hatch.build.targets.wheel]
include = ["imghdr.py"]
